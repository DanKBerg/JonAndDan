<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/textView"
        android:layout_width="238dp"
        android:layout_height="134dp"
        android:layout_marginStart="84dp"
        android:layout_marginTop="52dp"
        android:fontFamily="@font/amarante"
        android:text="@string/TitleText"
        android:textAlignment="center"
        android:textColor="#E42A2A"
        android:textSize="30sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/textView2"
        android:layout_width="197dp"
        android:layout_height="107dp"
        android:layout_marginStart="104dp"
        android:layout_marginTop="104dp"
        android:fontFamily="@font/amarante"
        android:text="@string/Pressswordtext"
        android:textAlignment="center"
        android:textColor="#5B77DF"
        android:textSize="20sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/textView" />

    <ImageButton
        android:id="@+id/imageButton"
        android:layout_width="223dp"
        android:layout_height="272dp"
        android:layout_marginStart="92dp"
        android:layout_marginTop="20dp"
        android:background="#00DDF3D2"
        android:contentDescription="@string/Swords"
        android:onClick="Calender"
        android:scaleType="centerInside"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/textView2"
        app:srcCompat="@drawable/swords" />

</androidx.constraintlayout.widget.ConstraintLayout>